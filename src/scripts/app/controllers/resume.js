'use strict';

/**
 * @ngdoc function
 * @name dhcomApp.controller:ResumeCtrl
 * @description
 * # ResumeCtrl
 * Controller of the dhcomApp
 */
angular.module('dhcomApp')
  .controller('ResumeCtrl', function ($scope) {
    $scope.resumeThings = [
    ];
  });
